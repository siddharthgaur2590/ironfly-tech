Subscribe Execution Flow:

1. User can make a request using /api/stocks/subscribe carrying the stock data in the forms ticker illustration having details about companyName, ticker, price, volume, time.
2. The above context is recognized by rest controller class of microservice architecture using defined API pattern.
3. Then it calls the serviceImple calls for business logic execution to save the data to the repo using JPA database connections.
4. Mapper class is used to transform the data recognizable for JPA repo.
5. Model classed are used to stream the data from end-to-end.

Un-Subscribe Execution Flow:

1. It follows the api calling method as mentioned above.
2. The logic would be derived of the fact to unsubscribe the stock using id.
3. Deletion of stock can be managed through this service /api/stocks/unsubscribe
